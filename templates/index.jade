extends layout

block vars
  - var bodyclass = 'index';


block content
  script(type='text/javascript' src='scripts/init.js')
  include author
  each article in articles
    header
      p.date
        span= moment.utc(article.date).format('DD. MMMM YYYY')
      h2
        //- use link on other websites
        //- use url for current blog
        a(href=article.metadata.link || article.url)= article.title
        if article.metadata.icon
          span.icon= article.metadata.icon
    unless article.metadata.link || article.metadata.no_intro
      section.content
        | !{ typogr(article.intro).typogrify() }
        if article.hasMore
          p.more
            a(href=article.url) далее
block prepend footer
  div.nav
    if prevPage
      a(href=prevPage.url) « Назад
    else
      a(href='/archive.html') « Архив
    if nextPage
      a(href=nextPage.url) Следующая страница »
  section.twitter
    .twitter-wrapper
      a(class="twitter-timeline", href="https://twitter.com/is_ruslan", data-lang="ru", data-tweet-limit="1", data-chrome="nofooter" data-widget-id="506163152916975618") Твиты пользователя @is_ruslan
      <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?'http':'https';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+"://platform.twitter.com/widgets.js";fjs.parentNode.insertBefore(js,fjs);}}(document,"script","twitter-wjs");</script>

  section.instagram
    span.title Инстаграм
    #instafeed
    span#load-more Еще







